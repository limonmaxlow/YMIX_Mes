# Server configuration
server.address=0.0.0.0
server.port=8080

# Database configuration - ИЗМЕНИТЕ НА localhost
spring.datasource.url=jdbc:postgresql://localhost:5432/messenger_db
spring.datasource.username=limon
spring.datasource.password=l_i_m_o_n

# JPA configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

# Logging
logging.level.com.messenger=DEBUG